<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>FreshMart - Home</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Header & Logo */
        header { background:#131921; color:white; }
        .logo img { height:50px; object-fit:contain; }
        /* Search */
        .search-input { width:100%; padding:8px 12px; border-radius:4px 0 0 4px; border:none; }
        .search-btn { border-radius:0 4px 4px 0; background:#febd69; color:#111; font-weight:600; border:none; }
        .search-btn:hover { background:#f3a847; }
        /* Icons */
        .header-btn { color:white; margin:0 5px; }
        .header-btn:hover { color:#febd69; }
        .cart-count { background:#ff9900; color:#111; padding:2px 6px; font-size:12px; border-radius:50%; position:absolute; top:-5px; right:-5px; }
        /* Categories nav */
        .categories-nav { background:#232f3e; padding:10px 0; overflow-x:auto; white-space:nowrap; }
        .categories-nav .btn { flex-shrink:0; margin-right:5px; }
        /* Offer Section */
        .offer-card img { height:150px; object-fit:contain; }
        .offer-scroll { overflow-x:auto; display:flex; gap:10px; padding-bottom:10px; }
        .offer-card { min-width:200px; border:1px solid #ddd; border-radius:5px; padding:10px; text-align:center; }
        /* Product Section */
        .product-card img { height:200px; object-fit:contain; }
        /* Responsive */
        @media (max-width:768px) {
            .search-bar { width:100%; margin-top:8px; margin-bottom:8px; }
            header .d-flex { flex-direction:column; align-items:flex-start; gap:8px; }
            .categories-nav { padding:5px 0; }
        }
    </style>
</head>
<body>

<!-- Header -->
<header class="sticky-top py-2 shadow-sm">
    <div class="container-fluid d-flex align-items-center justify-content-between flex-wrap">

        <!-- Logo -->
        <a href="${pageContext.request.contextPath}/" class="d-flex align-items-center logo me-3">
            <img src="${pageContext.request.contextPath}/assets/img/download.png" alt="FreshMart Logo">
            <span class="ms-2 fw-bold fs-4 text-white">FreshMart</span>
        </a>

        <!-- Search Bar -->
        <form class="flex-grow-1 d-flex search-bar me-3">
            <input type="text" class="form-control search-input" placeholder="Search for products, brands and more">
            <button type="submit" class="btn search-btn"><i class="fas fa-search"></i></button>
        </form>

        <!-- Right Icons -->
        <div class="d-flex align-items-center flex-wrap gap-2">

            <!-- Account -->
            <c:if test="${empty sessionScope.user}">
                <div class="dropdown">
                    <a class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown"><i class="fas fa-user"></i> Account</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="${pageContext.request.contextPath}/Login">Login</a></li>
                        <li><a class="dropdown-item" href="${pageContext.request.contextPath}/register">Register</a></li>
                    </ul>
                </div>
            </c:if>
            <c:if test="${not empty sessionScope.user}">
                <div class="dropdown">
                    <a class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown"><i class="fas fa-user"></i> ${sessionScope.user.name}</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="${pageContext.request.contextPath}/profile">My Profile</a></li>
                        <li><a class="dropdown-item" href="${pageContext.request.contextPath}/logout">Logout</a></li>
                    </ul>
                </div>
            </c:if>

            <!-- Wishlist & Cart -->
            <a href="${pageContext.request.contextPath}/wishlist" class="btn btn-dark position-relative"><i class="fas fa-heart"></i></a>
            <a href="${pageContext.request.contextPath}/cart" class="btn btn-dark position-relative">
                <i class="fas fa-shopping-cart"></i>
                <span class="cart-count">3</span>
            </a>
        </div>
    </div>

    <!-- Categories -->
    <div class="categories-nav container-fluid">
        <div class="d-flex gap-2">
            <div class="dropdown flex-shrink-0">
                <a class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown"><i class="fas fa-bars me-1"></i> All Categories</a>
                <ul class="dropdown-menu">
                    <c:forEach var="c" items="${categories}">
                        <li><a class="dropdown-item" href="${pageContext.request.contextPath}/products?categories=${c.name}">${c.name}</a></li>
                    </c:forEach>
                </ul>
            </div>
            <a href="${pageContext.request.contextPath}/products" class="btn btn-dark flex-shrink-0">Products</a>
            <a href="${pageContext.request.contextPath}/offers" class="btn btn-dark flex-shrink-0">Offers</a>
            <a href="${pageContext.request.contextPath}/customer-service" class="btn btn-dark flex-shrink-0">Customer Service</a>
            <a href="${pageContext.request.contextPath}/blog" class="btn btn-dark flex-shrink-0">Blog</a>
        </div>
    </div>
</header>

<!-- Offer Section -->
<div class="container my-4">
    <h3 class="mb-3">🔥 Top Offers</h3>
    <div class="offer-scroll">
        <c:forEach var="o" items="${offerProducts}">
            <div class="offer-card">
                <img src="${pageContext.request.contextPath}/assets/img/product_img/${o.image}" alt="${o.title}">
                <p class="mt-2 fw-bold text-dark">${o.title}</p>
                <div>
                    <span class="text-danger fw-bold">₹${o.discountPrice}</span>
                    <span class="text-decoration-line-through text-muted ms-1">₹${o.price}</span>
                    <span class="text-success ms-1 fw-bold">${o.discount}% OFF</span>
                </div>
                <c:choose>
                    <c:when test="${o.stock > 0}">
                        <a href="${pageContext.request.contextPath}/user/addCart?pid=${o.id}&uid=${sessionScope.user.id}" class="btn btn-primary btn-sm mt-2">Add to Cart</a>
                    </c:when>
                    <c:otherwise>
                        <button class="btn btn-warning btn-sm mt-2" disabled>Out of Stock</button>
                    </c:otherwise>
                </c:choose>
            </div>
        </c:forEach>
    </div>
</div>

<!-- Latest Products Section -->
<div class="container my-4">
    <h3 class="mb-3">🛒 Latest Products</h3>
    <div class="row">
        <c:forEach var="p" items="${products}">
            <div class="col-6 col-md-3 mb-3">
                <div class="card product-card p-2">
                    <img src="${pageContext.request.contextPath}/assets/img/product_img/${p.image}" class="card-img-top" alt="${p.title}">
                    <div class="card-body p-2 text-center">
                        <p class="fw-bold">${p.title}</p>
                        <p>
                            <span class="text-danger fw-bold">₹${p.discountPrice}</span>
                            <span class="text-decoration-line-through text-muted ms-1">₹${p.price}</span>
                            <span class="text-success ms-1 fw-bold">${p.discount}% OFF</span>
                        </p>
                        <c:choose>
                            <c:when test="${p.stock > 0}">
                                <a href="${pageContext.request.contextPath}/user/addCart?pid=${p.id}&uid=${sessionScope.user.id}" class="btn btn-primary btn-sm">Add to Cart</a>
                            </c:when>
                            <c:otherwise>
                                <button class="btn btn-warning btn-sm" disabled>Out of Stock</button>
                            </c:otherwise>
                        </c:choose>
                    </div>
                </div>
            </div>
        </c:forEach>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
