<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products - FreshMart Premium</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
    
    <style>
        body {
            background: linear-gradient(-45deg, #f2a1a1, #f7c8c8, #a3d1f5, #b2e0f7);
            background-size: 400% 400%;
            animation: gradient 20s ease infinite;
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
        }
        @keyframes gradient { 0% { background-position:0% 50%; } 50% { background-position:100% 50%; } 100% { background-position:0% 50%; } }
        .product-container { background: rgba(255,255,255,0.9); backdrop-filter: blur(15px); border-radius: 20px; padding: 25px; margin-top: 30px; box-shadow: 0 8px 32px rgba(0,0,0,0.15);}
        .section-title { text-align:center; font-weight:800; font-size:32px; color:#2a2d3b; margin-bottom:25px; position:relative; }
        .section-title::after { content:''; width:70px; height:5px; background:linear-gradient(135deg,#667eea,#764ba2); position:absolute; bottom:-10px; left:50%; transform:translateX(-50%); border-radius:5px; }
        .category-side { background: rgba(255,255,255,0.95); backdrop-filter: blur(10px); border-radius: 15px; box-shadow: 0 5px 20px rgba(0,0,0,0.1); margin-left: -100px !important; margin-right: 115px; }
        .category-head { background: linear-gradient(135deg,#667eea,#764ba2); color:white; padding: 12px; font-weight:700; border-radius: 15px 15px 0 0; }
        .cat-item { padding: 12px 20px; border-bottom:1px solid #ececec; font-weight:600; cursor:pointer; text-decoration:none; display:block; color:#2a2d3b; transition:0.3s; }
        .cat-item:hover, .cat-item.active { background: rgba(102,126,234,0.15); border-left:5px solid #667eea; color:#667eea; }
        .product-card { border-radius:15px; background:rgba(255,255,255,0.95); box-shadow:0 5px 15px rgba(0,0,0,0.1); transition:0.4s; }
        .product-card:hover { transform:translateY(-10px) scale(1.03); box-shadow:0 15px 30px rgba(0,0,0,0.2); }
        .product-img { height:180px; object-fit:contain; padding:15px; transition:0.4s; }
        .product-card:hover .product-img { transform:scale(1.1); }
        .product-title { font-size:18px; font-weight:600; color:#2a2d3b; }
        .price-box { margin-top:10px; }
        .price-new { font-size:20px; font-weight:700; color:#444; }
        .price-old { text-decoration:line-through; color:#888; margin-left:8px; }
        .discount { color:#28a745; font-weight:700; margin-left:5px; }
        .btn-view { margin-top:10px; background:linear-gradient(135deg,#667eea,#764ba2); border:none; color:white; padding:8px 20px; border-radius:10px; transition:.3s; font-weight:600; }
        .btn-view:hover { transform:scale(1.05); background:linear-gradient(135deg,#5a6fde,#6c3aa8); }
    </style>
</head>

<body>

<%@ include file="header1.jsp" %>

<div class="container">
    <div class="row">
        <!-- CATEGORY SIDEBAR -->
        <div class="col-lg-3 col-md-4 mt-4">
            <div class="category-side">
                <div class="category-head">Categories</div>
                <a href="/products" class="cat-item ${empty param.categories ? 'active' : ''}">All Products</a>
                <c:forEach var="c" items="${categories}">
                    <a href="/products?categories=${c.name}" class="cat-item ${param.categories eq c.name ? 'active' : ''}">${c.name}</a>
                </c:forEach>
            </div>
        </div>

        <!-- PRODUCT SECTION -->
        <div class="col-lg-9 col-md-8">
            <div class="product-container">
                
                <c:choose>
                    <!-- SINGLE PRODUCT VIEW -->
                    <c:when test="${viewType eq 'SINGLE'}">
                        <h2 class="section-title">${product.title}</h2>
                        <div class="row g-4">
                            <div class="col-lg-4 col-md-6">
                                <div class="product-card p-3 text-center">
                                    <img src="../assets/img/product_img/${product.image}" class="product-img" alt="${product.title}">
                                    <h5 class="product-title mt-2">${product.title}</h5>
                                    <div class="price-box">
                                        <span class="price-new">₹${product.discountPrice}</span>
                                        <span class="price-old">₹${product.price}</span>
                                        <span class="discount">${product.discount}% OFF</span>
                                    </div>
                                    <button class="btn btn-view">Add to Cart</button>
                                    <div class="mt-3 text-start">
                                        <p><strong>Description:</strong> ${product.description}</p>
                                        <p><strong>Category:</strong> ${product.category.name}</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </c:when>

                    <!-- ALL PRODUCTS LIST -->
                    <c:otherwise>
                        <h2 class="section-title">Products</h2>
                        <div class="row g-4">
                            <c:forEach var="p" items="${products}">
                                <div class="col-lg-4 col-md-6">
                                    <div class="product-card p-3 text-center">
                                        <img src="../assets/img/product_img/${p.image}" class="product-img" alt="${p.title}">
                                        <h5 class="product-title mt-2">${p.title}</h5>
                                        <div class="price-box">
                                            <span class="price-new">₹${p.discountPrice}</span>
                                            <span class="price-old">₹${p.price}</span>
                                            <span class="discount">${p.discount}% OFF</span>
                                        </div>
                                        <a href="/products/${p.id}" class="btn btn-view">View Details</a>
                                    </div>
                                </div>
                            </c:forEach>
                        </div>
                    </c:otherwise>
                </c:choose>

            </div>
        </div>
    </div>
</div>

<%@ include file="Footar.jsp" %>
</body>
</html>
