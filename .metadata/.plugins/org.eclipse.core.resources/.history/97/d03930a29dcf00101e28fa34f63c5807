<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%>
<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Apna Shop - Premium Shopping Experience</title>

<!-- Bootstrap -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<style>

/* ---------------- HERO SECTION ---------------- */
.fresh-hero {
    height: 520px;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
}

.fresh-slider {
    position: absolute;
    width: 500%;
    height: 100%;
    display: flex;
    animation: scrollSlider 45s linear infinite;
}

.slide { min-width: 100%; height: 100%; }
.slide img { width: 100%; height: 100%; object-fit: cover; filter: brightness(0.75); }

@keyframes scrollSlider {
    0% { transform: translateX(0); }
    20% { transform: translateX(-100%); }
    40% { transform: translateX(-200%); }
    60% { transform: translateX(-300%); }
    80% { transform: translateX(-400%); }
    100% { transform: translateX(0); }
}

.fresh-content {
    position: absolute;
    text-align: center;
    color: white;
    padding: 20px 30px;
    border-radius: 12px;
    backdrop-filter: blur(4px);
    animation: fadeIn 1.4s ease;
}

.fresh-content h1 { font-size: 50px; font-weight: 800; }
.fresh-btn { padding: 14px 35px; background: #ffcf2b; font-weight: bold; border-radius: 10px; color: #111; text-decoration: none; }

@keyframes fadeIn { from { opacity: 0; transform: translateY(25px); } to { opacity: 1; transform: translateY(0); } }

/* ---------------- BACKGROUND ---------------- */
body {
    background: linear-gradient(-45deg, #f2a1a1, #f7c8c8, #a3d1f5, #b2e0f7);
    background-size: 400% 400%;
    animation: gradient 20s ease infinite;
    font-family: 'Poppins', sans-serif;
}

@keyframes gradient { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }

/* ---------------- MAIN CONTAINER ---------------- */
.main-container {
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(15px);
    border-radius: 20px;
    margin: 25px auto;
    padding: 25px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.2);
}

/* SECTION TITLE */
.section-title {
    text-align: center;
    font-weight: 700;
    color: #2a2d3b;
    margin-bottom: 25px;
    position: relative;
}

.section-title::after {
    content: '';
    width: 60px;
    height: 4px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
}

/* ---------------- CARD STYLES ---------------- */
.offer-card, .category-card, .product-card {
    background: white;
    border-radius: 18px;
    padding: 15px;
    text-align: center;
    box-shadow: 0 6px 18px rgba(0,0,0,0.15);
    transition: .3s;
    position: relative;
}

.offer-card:hover, .category-card:hover, .product-card:hover {
    transform: translateY(-10px);
}

/* IMAGES */
.offer-img { height: 140px; width: 100%; object-fit: contain; }
.category-img { height: 120px; width: 100%; object-fit: contain; }
.product-img { height: 180px; width: 100%; object-fit: contain; }

/* DISCOUNT BADGE */
.discount-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    background: #ff4848;
    color: white;
    font-size: 12px;
    font-weight: bold;
    padding: 5px 10px;
    border-radius: 8px;
}

/* ADD BUTTON */
.btn-addcart {
    padding: 6px 18px;
    border-radius: 10px;
    background: #4a9cff;
    border: none;
    color: white;
    font-weight: bold;
    transition: .3s;
}

.btn-addcart:hover { background: #2f7ce0; }

</style>
</head>

<body>

<%@ include file="header1.jsp" %>

<!-- ===================== HERO SECTION ===================== -->
<section class="fresh-hero">
    <div class="fresh-slider">
        <div class="slide"><img src="${pageContext.request.contextPath}/assets/kisan.jpg"></div>
        <div class="slide"><img src="${pageContext.request.contextPath}/assets/kkkk.jpeg"></div>
        <div class="slide"><img src="${pageContext.request.contextPath}/assets/kisan.jpg"></div>
        <div class="slide"><img src="${pageContext.request.contextPath}/assets/beg.jpeg"></div>
        <div class="slide"><img src="${pageContext.request.contextPath}/assets/vegi.jpg"></div>
    </div>
    <div class="fresh-content">
        <h1>FreshMart – Freshness Delivered</h1>
        <p>Vegetables, fruits, dairy, snacks & more with premium quality.</p>
        <a href="#" class="fresh-btn">Shop Now</a>
    </div>
</section>

<div class="main-container container">

    <!-- ===================== OFFERS ===================== -->
    <%-- <div class="bg-section">
        <h2 class="section-title">🔥 Top Offers</h2>
        <div class="row g-4">
            <c:forEach var="o" items="${offerProducts}">
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="offer-card">
                        Calculate Discount %
                        <%
                            double price = o.getPrice();
                            double discountPrice = o.getDiscountPrice();
                            int discountPercent = (int)Math.round(((price - discountPrice)/price)*100);
                        %>
                        <span class="discount-badge"><%=discountPercent%>% OFF</span>
                        <img src="${pageContext.request.contextPath}/assets/img/product_img/${o.image}" class="offer-img" alt="${o.title}">
                        <p class="mt-2 fw-bold">${o.title}</p>
                        <div>
                            <span class="text-danger fw-bold">₹${o.discountPrice}</span>
                            <span class="text-decoration-line-through text-muted ms-1">₹${o.price}</span>
                            <span class="text-success ms-1 fw-bold"><%=discountPercent%>% OFF</span>
                        </div>
                        <button class="btn-addcart mt-2">Add</button>
                    </div>
                </div>
            </c:forEach>
        </div>
    </div>
 --%>
 <c:forEach var="o" items="${offerProducts}">
    <div class="offer-card">
        <c:set var="discountPercent" value="${((o.price - o.discountPrice) / o.price) * 100}" />
        <span class="discount-badge">${fn:substringBefore(discountPercent, '.')}% OFF</span>

        <img src="${pageContext.request.contextPath}/assets/img/product_img/${o.image}" class="offer-img" alt="${o.title}">
        <p class="mt-2 fw-bold">${o.title}</p>
        <div>
            <span class="text-danger fw-bold">₹${o.discountPrice}</span>
            <span class="text-decoration-line-through text-muted ms-1">₹${o.price}</span>
            <span class="text-success ms-1 fw-bold">${fn:substringBefore(discountPercent, '.')}% OFF</span>
        </div>
        <button class="btn-addcart mt-2">Add</button>
    </div>
</c:forEach>
    <!-- ===================== CATEGORIES ===================== -->
    <div class="bg-section">
        <h2 class="section-title">Shop by Category</h2>
        <div class="row g-4">
            <c:forEach var="c" items="${categories}">
                <div class="col-6 col-md-4 col-lg-2">
                    <div class="category-card">
                        <img src="${pageContext.request.contextPath}/assets/img/category_img/${c.imageName}" class="category-img" alt="${c.name}">
                        <p class="mt-2 fw-bold">
                            <a href="products?categories=${c.name}" class="text-dark text-decoration-none">${c.name}</a>
                        </p>
                    </div>
                </div>
            </c:forEach>
        </div>
    </div>

    <!-- ===================== LATEST PRODUCTS ===================== -->
    <div class="bg-section">
        <h2 class="section-title">Latest Products</h2>
        <div class="row g-4">
            <c:forEach var="p" items="${products}">
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="product-card">
                        <span class="discount-badge">${p.discount}% OFF</span>
                        <img src="${pageContext.request.contextPath}/assets/img/product_img/${p.image}" class="product-img" alt="${p.title}">
                        <p class="mt-2 fw-bold">
                            <a href="${pageContext.request.contextPath}/products?id=${p.id}" class="text-dark text-decoration-none">${p.title}</a>
                        </p>
                        <div class="mb-2">
                            <span class="text-danger fw-bold">₹${p.discountPrice}</span>
                            <span class="text-decoration-line-through text-muted ms-1">₹${p.price}</span>
                        </div>
                        <button class="btn-addcart">Add</button>
                    </div>
                </div>
            </c:forEach>
        </div>
    </div>

</div>

<%@ include file="Footar.jsp" %>

</body>
</html>
